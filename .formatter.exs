# Used by "mix format"
[
  import_deps: [:ecto, :ecto_sql, :phoenix],
  inputs: [
    "mix.exs",
    "config/*.exs",
    "lib/**/*.{ex,exs}",
    "test/**/*.{ex,exs}",
    "priv/repo/migrations/*.exs"
  ]
]
